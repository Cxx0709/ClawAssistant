# 2026-09-04

## ClawAssistant 参赛方向与概念设计
- 确认参赛主题：方向一/主题1「大模型驱动的智能交互与对话系统」，主题3（多模态）作为演示加分项而非主叙事。
- 确定项目新意叙事：「从被动应答，到主动干活」三创新点 = 对话×工作台双主体 + Agent 雷达（主动性可视化）+ 推理时间线（ReAct 透明化）。
- 在 Ardot 画布产出三块 1920×1080 概念画板（文件 id 722239259249457）：
  - 01 对话×工作台：导航栏/对话流（工具痕迹 chip、快捷建议）/行程看板卡（可编辑清单+AI 氛围插画）。
  - 02 Agent 雷达：SVG 雷达图（四类盯守任务 blip）+「你不在的时候」时间线（6 事件）+ 本周统计。
  - 03 推理时间线：ReAct 四步骤卡 + 折叠行 + 长期记忆引用卡 + 安全边界卡 + 「追问→转为持续任务」衔接画板02。
- 设计系统：浅紫工作台底 #F3F2FA + 白卡 + 单一信号色 #5B5BD6（iris），成功 #1E9E7A / 警示 #B97A14；中文 Sarasa Gothic SC，数字 Roboto Mono。变量集 Tokens 已建。
- 踩坑：Ardot SVG 解析器对部分 svg 反复报 degenerate geometry（同样内容有时成功有时失败）；兜底方案=基础图形节点拼装或先插根节点再 M 移入。capture_layout 根节点只能传 frame 不能传 page。
- 待办：用户确认概念后可继续做答辩 PPT 或落地方案拆解。
- 补充产出：设计文档 `docs/设计文档-主题1概念设计.md`（v1.0）——产品叙事/设计语言(tokens+字体)/三画板布局与内容规格/7 条交互行为规格(I-1~I-7)/前端实现映射(对接 React+SSE+outbox+artifact)/答辩素材摘录。实现优先级：工作台联动 > 雷达时间线 > 推理时间线 > SVG 雷达。
- 落地改造核对（对照实际代码后产出 `docs/主题1落地改造方案.md`）：后端底座大多已就绪（growth_goal/scheduled_task/notification_outbox/agent_activity/chat_runs+tool_trace_json，且 /runs/{id} 已返回工具轨迹）。关键缺口=① 前端无独立工作台/雷达/推理页（App.tsx 用 query 切 view，无路由）；② TravelPlanOption.itinerarySummary 是叙述文本，无 title/time/status(done|adjusted|added) 结构化逐项，也无按天分组，前端未展示行程；③ ToolTraceItem 只存简短中文 detail，无 thought/reflection/工具入参与结果报文（ReAct 分步需落库增强）；④ Tailwind 主题是 ChatGPT 绿 brand #10a37f，需加 accent紫#5B5BD6/ok绿/warn琥珀 token。落地顺序建议：A token+抽 ArtifactCard → C 时间线/推理(成本最低,复用 agent_activity+/runs) → B 工作台+行程看板(答辩第一屏,量最大) → SVG 雷达留二期。

## 打车(didi_ride)功能彻底移除（本轮）
- 背景：工作区未提交删除进行中（scout信息猎手+didi打车 core 已删），用户要求"继续删打车"，确认选择"打车彻底移除"。
- 已在先前删除基础上补齐删除：SkillPendingCoordinator 移除 didi_ride reaction/RIDE_ESTIMATE_CONFIRM/handleDidiRide，构造改无参；PendingToolCoordinator 移除 didi_ride 显示名；SkillPendingCoordinatorTest 删 3 个 didi 测试；application.properties(.example) 移除 didi.mcp.* 配置块及真实 api-key。
- 路由层：TransportTriggerPolicy TRANSPORT_VERBS 去除 打车/叫车/代驾，仅留 坐车/怎么去/怎么走/坐地铁/坐公交；SimpleChatClassifier、SemanticTriggerPolicy、SkillRouter 注释去除打车字样；相关测试 TransportTriggerPolicyTest/SkillRouterExplicitSwitchTest/ToolAssemblyCrossSkillTest 改写为交通方式用例。
- 保留范围：transport 技能仅保留 transport_recommend(交通方式推荐)+地图工具，skills.yml/skill-triggers.yml/transport.txt 本已无打车。
- 验证：mvn -o clean test-compile BUILD SUCCESS（377 主+102 测试）；受影响测试全绿（40 run 0 fail）。src/main 已无 打车/叫车/代驾/滴滴/didi 引用。
- maven 无 mvnw，须用 `java -Dmaven.home=D:/maven/apache-maven-3.9.9 -Dmaven.multiModuleProjectDirectory=D:/Ucode/ClawAssistant -Dclassworlds.conf=D:/maven/apache-maven-3.9.9/bin/m2.conf -classpath D:/maven/apache-maven-3.9.9/boot/plexus-classworlds-2.8.0.jar org.codehaus.plexus.classworlds.launcher.Launcher -o ...`（Windows 路径，unix 路径会让 classworlds 找不到类）。

## 行程看板(BOARD)展示层彻底移除（接上轮打车后）
- 用户确认「行程看板没必要」，选择只删看板展示层、保留 travel 出行功能与逐日行程数据(itineraryItems/ItinerarySummary)。
- 后端删除：WorkspaceController.java 整个文件；ArtifactKind 移除 BOARD 枚举值；GeneratedArtifact 移除 boardData 字段 + board() 工厂（同步修 ArtifactService 3 处构造调用 + suggestFileName 的 BOARD case）；ArtifactCollector 移除 emitBoard 默认方法；RequestArtifactCollector 移除 emitBoard 覆写；TravelSelectOptionTool 移除 BOARD 产出块 + buildBoardData + 未用 stateStore/JsonNode/ArrayNode/TravelPlanDraft/TravelPlanOption/ArtifactCollector 导入；ChatApplicationService 移除 case BOARD。
- 前端删除：WorkspaceCanvas.tsx 移除「行程看板」标签页、board 状态/加载/boardToArtifact、props(conversationId/refreshToken/highlightArtifactId) 与未用 ArtifactCard 导入；ArtifactCard.tsx 移除 BoardCard 组件 + BOARD case + onBoardClick prop；api.ts 移除 Board* 类型 + getWorkspaceBoards/updateBoardItem（连同整个「工作台」分区）；types.ts 移除 BOARD 类型与 boardData/BoardView；ChatPage.tsx 移除 ArtifactList 的 onOpenWorkspace prop 与 MessageList 的 onOpenWorkspace 链路、删除 highlightArtifactId state、WorkspaceCanvas 调用简化为无参。
- 验证：mvn clean test-compile BUILD SUCCESS（102 测试源全过）；ArtifactServiceTest 1/1 通过；tsc --noEmit EXIT=0；travel/itinerary 数据未受影响。
- maven 启动方式沿用：java -Dmaven.home=D:/maven/apache-maven-3.9.9 -Dmaven.multiModuleProjectDirectory=D:/Ucode/ClawAssistant -Dclassworlds.conf=D:/maven/apache-maven-3.9.9/bin/m2.conf -classpath D:/maven/apache-maven-3.9.9/boot/plexus-classworlds-2.8.0.jar org.codehaus.plexus.classworlds.launcher.Launcher -o ...（须用 unix 路径不要 Windows 反斜杠）。

## 信息猎手(scout)彻底清理（接看板移除后）
- 用户确认「清」：scout 配置/防御钩子/空目录/注释引用全部移除，保留 SkillPendingCoordinator 注册表模式骨架以备未来扩展。
- 配置：application.properties 删除 Information Scout Agent 段(29行 scout.*) + workflow.scout.cron + scout.rss.*（12 条 RSS）；application.properties.example 同删整段。
- 代码：SkillPendingCoordinator 删除 START_INFORMATION_SCOUT 常量与 information_scout 反应 + 防御性钩子注释（保留注册表骨架）；ExecutionLoop / LoopSuspensionPolicy / SkillRouter / TransportTriggerPolicy / GrowthGoalRepository / SearchService 注释去除「scout/信息猎手」字样；SearchService 删除 searchByDate(int) 与 searchByDate(int,int) 两个 scout 专用的死方法（无人调用）。
- 空目录：rmdir tool/scout（main+test）。
- 测试占位符重命名：7 个测试文件里的 information-scout/scoutTriggerPolicy/scoutWorkflow/信息猎手/START_INFORMATION_SCOUT 等全部 → research/researchTriggerPolicy/researchWorkflow/信息研究/START_RESEARCH 等通用占位符（SkillExecutionDispatcherTest/SkillRegistryTest/SkillRouterPendingActionTest/SkillRouterSessionReleaseTest/SkillRoutingIsolationTest/SkillSessionPendingTest/ToolAssemblyCrossSkillTest）。
- 删除测试：SkillPendingCoordinatorTest.java（仅测试 information_scout 反应，scout 已删失去意义）。
- 验证：mvn clean test-compile BUILD SUCCESS（377 主源 + 101 测试源，因删测试 -1）；npm run typecheck EXIT=0；7 个改名测试类全绿（35/35）；全仓库 grep scout/信息猎手/START_INFORMATION_SCOUT 零残留。
- maven 启动同前：java -Dmaven.home=D:/maven/apache-maven-3.9.9 -Dmaven.multiModuleProjectDirectory=D:/Ucode/ClawAssistant -Dclassworlds.conf=D:/maven/apache-maven-3.9.9/bin/m2.conf -classpath D:/maven/apache-maven-3.9.9/boot/plexus-classworlds-2.8.0.jar org.codehaus.plexus.classworlds.launcher.Launcher -o ...（unix 路径）。
