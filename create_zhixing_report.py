from docx import Document
from docx.shared import Pt
from docx.enum.text import WD_ALIGN_PARAGRAPH
from docx.oxml import OxmlElement
from docx.oxml.ns import qn

src = r'D:\Downloads\附件2：作品赛竞赛模板.docx'
out = r'D:\Temp\ClawAssistant\知行作品报告书_参考稿融合版.docx'
doc = Document(src)

def set_text(p, text, bold=False, size=10.5):
    p.clear(); r=p.add_run(text); r.bold=bold; r.font.name='宋体'; r._element.rPr.rFonts.set(qn('w:eastAsia'),'宋体'); r.font.size=Pt(size)

set_text(doc.paragraphs[3], '2026年安徽省AI大模型创新应用竞赛', True, 16)
set_text(doc.paragraphs[4], '作品赛赛道', True, 16)
set_text(doc.paragraphs[17], '作品名称：知行（ZHIXING AGENT）', True, 14)

for p in doc.paragraphs:
    if p.style.name == 'Normal':
        for r in p.runs:
            r.font.name='宋体'; r._element.rPr.rFonts.set(qn('w:eastAsia'),'宋体'); r.font.size=Pt(10.5)

table=doc.tables[0]
vals = {
0: '知行（ZHIXING AGENT）',
1: '人工智能大模型创新应用',
2: '作品简介',
4: '知行是一套面向个人用户的多模态 Web 智能助理。用户可以通过浏览器输入文字、上传图片、语音和文件，把生活、学习与工作中的复杂任务交给知行处理。系统以 ReAct Agent 为核心，结合可配置 Skill、40 余类 Tool、长期记忆、目标跟进和主动通知能力，完成信息检索、天气与出行查询、旅行规划、校园课表、文件处理、图像理解、语音转写和内容生成等任务。项目采用 React 18 + TypeScript 前端与 Spring Boot 3.3 + Java 21 后端，接入 Qwen/DashScope 文本、视觉、ASR、TTS、图像模型，并以 SQLite 保存业务状态、以 Qdrant 支撑向量记忆和 Skill 知识检索。系统通过 SSE 实现对话流式输出和用户级通知推送，通过 app_user.id 实现多用户数据隔离。',
5: '自主研发的 Web 智能助理项目，围绕多模态交互、可组合 Agent 执行和长期陪伴式服务进行设计与实现。',
6: '智能办公与个人效率、教育与校园服务、旅行与生活服务等需要自然语言交互和任务自动化的场景。',
7: '用户在授权范围内输入的对话、附件和任务数据；系统配置的提示词、Skill 定义、确定性触发规则；公开天气、地图、搜索、动漫信息等接口数据；模型服务返回的文本、向量和多模态结果。',
8: 'Web 应用：知行（ZHIXING AGENT）。正式提交时可补充演示网址、运行截图或演示视频。'
}
for i,v in vals.items():
    if i < len(table.rows):
        cell=table.rows[i].cells[1]
        cell.text=v
        for p in cell.paragraphs:
            for r in p.runs:
                r.font.name='宋体'; r._element.rPr.rFonts.set(qn('w:eastAsia'),'宋体'); r.font.size=Pt(10.5)

def add_heading(text):
    p=doc.add_paragraph(); set_text(p,text,True,14); return p
def add_body(text):
    p=doc.add_paragraph(); set_text(p,text,False,10.5); p.paragraph_format.line_spacing=1.5; return p

add_heading('正文')
add_heading('一 选题定位')
add_body('当前的智能助手往往停留在一次性问答：用户需要重复说明背景，模型能够生成建议，却不能持续保存上下文、调用真实工具并跟进任务。与此同时，个人用户面对学习安排、出行决策、文件处理和信息订阅等事务时，往往需要在多个应用之间切换。知行的选题定位是把大模型从“回答问题的窗口”升级为“能够理解上下文、调用工具、保存状态并主动跟进的个人 Web 智能体”。')
add_body('作品面向需要统一处理多来源信息的学生、职场人员和普通家庭用户，重点解决三类问题：复杂任务缺少连续执行能力，多模态资料难以在同一入口处理，提醒和长期目标难以形成闭环。')
add_heading('二 社会价值')
add_body('知行把文字、图片、语音和文件统一纳入同一工作台，降低了用户使用智能服务的门槛。对于学习场景，它可以处理课表、考试和提醒；对于生活场景，它可以组合天气、地图、交通和旅行工具；对于工作场景，它可以解析文档、生成文件并保留任务上下文。用户不必记忆复杂指令，只需要用自然语言描述目标，系统会在必要时追问关键信息。')
add_body('作品的推广价值来自可配置的 Skill 和 Tool 机制。新能力可以通过 Skill 定义、触发规则和工具适配接入，而不必重写整套对话系统。多用户隔离、通知 outbox、失败恢复和旧数据接管机制，也使其具备从个人试用向小型组织内部服务扩展的基础。')
add_heading('三 技术方案')
add_body('系统采用前后端分层的 Web 架构。React 18、TypeScript 和 Vite 构成客户端，负责登录注册、对话工作台、附件上传、工具轨迹、记忆、目标、角色和可视化页面；Spring Boot 负责认证、REST 接口、SSE 流式响应、业务编排与持久化。Spring Security 使用 Session、CSRF 和 BCrypt，所有业务请求通过当前用户身份获得统一的 app_user.id。')
add_body('对话请求首先进入 WebChatController，随后由 ChatApplicationService 校验会话归属、读取并解析附件、建立 AgentContext。ReActAgentExecutor 先恢复对话历史、摘要、长期记忆和已有计划，再由 SkillRouter 根据显式技能、关键词和会话状态选择能力范围。ExecutionLoop 驱动模型进行多轮思考与工具调用，SkillExecutionDispatcher 将可配置 Skill 分发给对应执行器；需要用户确认的工具会进入 WAIT_CONFIRM 状态，避免高影响操作被静默执行。')
add_body('模型层接入 DashScope OpenAI-compatible 文本模型，并分别使用 Vision、ASR、TTS 和 Image 能力处理图片、语音和图像生成。长期记忆和 Skill 知识库先经过 Embedding，再写入 Qdrant 进行相似检索；SQLite 保存用户、会话、上下文消息、摘要、计划、技能会话、通知 outbox 和可见运行记录；ArtifactService 将上传件和生成文件按用户隔离保存。')
add_body('提示词采用“系统提示词 + Skill 提示词 + 检索知识 + 用户上下文”的组合方式。系统提示词约束角色、工具调用和安全边界，Skill 提示词描述具体场景，检索结果补充知识，ContextBuilder 根据预算裁剪历史并加入长期记忆。对“记住……”类请求，ChatApplicationService 还提供与模型工具调用无关的自动保存路径，保证用户明确提出的记忆不会因模型漏调工具而丢失。')
add_body('系统实现中的主要难点包括长对话上下文控制、工具调用失败与重试、流式输出中断恢复、多用户数据隔离和外部服务不稳定。项目通过上下文预算管理、对话摘要、PlanState 持久化、运行状态表、SSE 清理机制、请求级 ArtifactCollector 和租户键约束进行处理。定时任务统一写入 notification_outbox，在线用户通过 NotificationStreamService 接收 per-user SSE，离线用户仍可在下次登录查看，并可通过 SMTP 获得提醒。')
add_heading('四 技术架构图')
add_body('下图展示知行从浏览器入口到 Agent 执行、数据存储和外部模型服务的主要关系。')
doc.add_paragraph().add_run().add_picture(r'D:\Temp\ClawAssistant\architecture.mmd', width=Pt(1)) if False else None
add_body('架构图源文件已随项目提供为 architecture.mmd，可在支持 Mermaid 的编辑器中渲染。')

# 校正模板行号并将正文替换成经源码核对的版本。
intro=('知行是一款运行在浏览器中的多模态个人智能助理。用户可以输入文字、上传图片、音频和文件，提出一个目标后由系统补齐必要信息，选择 Skill 与 Tool 处理天气、出行、课表、文件、记忆和目标等任务。系统以 ReAct Agent 组织模型与工具的多轮协作，使用 Qwen/DashScope 处理文本、视觉、语音和图像，以 SQLite 保存账户和任务状态，并以 Qdrant 支撑长期记忆与知识检索。对话界面通过 SSE 返回正文、工具轨迹和通知，把任务执行、结果展示和后续跟进放在同一工作台。')
rows={0:'知行（ZHIXING AGENT）',1:'',2:'作品简介',3:intro,4:'面向个人事务管理与多模态交互需求设计开发的 Web 智能助理应用。',5:'个人效率工具、校园生活服务、日常出行与信息管理。',6:'用户主动提交的对话与附件；公开信息接口；Skill 配置与领域知识；模型生成及向量化结果。',7:'浏览器端 Web 程序；支持文本、图片、音频与文件交互。'}
for i,t in rows.items():
    table.rows[i].cells[1].text=t
    table.rows[i].height=None
    for c in table.rows[i].cells:
        for p in c.paragraphs:
            set_text(p,p.text); p.paragraph_format.line_spacing=1.5
            p.paragraph_format.space_before=Pt(3); p.paragraph_format.space_after=Pt(3)

# 保留封面、填写说明和简介表，删除原模板正文提示以及早期草稿。
body=doc._element.body
following=False
for child in list(body):
    if child is table._element: following=True; continue
    if following and child.tag!=qn('w:sectPr'): body.remove(child)
for p in doc.paragraphs:
    if not p.text.strip():
        p.paragraph_format.space_before=Pt(0); p.paragraph_format.space_after=Pt(0)
        p.paragraph_format.line_spacing=1
        for r in p.runs: r.font.size=Pt(1)
    if p.text.strip() in ['填写说明','作品简介']:
        p.paragraph_format.page_break_before=True
        p.paragraph_format.space_before=Pt(0)
        p.paragraph_format.space_after=Pt(14)
for section in doc.sections:
    from docx.enum.section import WD_SECTION_START
    section.start_type=WD_SECTION_START.NEW_PAGE

def page(title):
    p=add_heading(title); p.paragraph_format.page_break_before=True
    p.paragraph_format.space_before=Pt(0); p.paragraph_format.space_after=Pt(12)
def sub(t):
    p=add_heading(t); p.runs[0].font.size=Pt(12)
    p.paragraph_format.keep_with_next=True
    p.paragraph_format.space_before=Pt(8); p.paragraph_format.space_after=Pt(5)
def para(t):
    p=add_body(t); p.paragraph_format.space_before=Pt(0); p.paragraph_format.space_after=Pt(6)
    p.paragraph_format.first_line_indent=Pt(21)
def figure(name,width=405):
    p=doc.add_paragraph(); p.paragraph_format.line_spacing=1
    p.alignment=WD_ALIGN_PARAGRAPH.CENTER
    p.add_run().add_picture('D:/Temp/ClawAssistant/知行文档配图/'+name+'.png',width=Pt(width))

page('一 选题定位与社会价值')
para('知行是一款运行在浏览器中的个人智能助理。它和只返回文字建议的聊天窗口不同：用户提出一个目标后，系统会补齐必要信息，判断需要哪些 Skill 和 Tool，执行后返回结果，并将本轮工具轨迹、状态和产物展示在工作台中。图片、音频和文件可以直接作为任务材料，任务状态、个人记忆和后续提醒则由系统持续保存和跟进。')
sub('1 需求与问题')
para('个人事务往往跨越多个应用：安排出行需要天气、地点和路线信息，管理课程需要解析文件并配置提醒，长期目标需要多次记录和复盘。仅获得一段建议并不能完成这些事务，用户仍需手动切换工具、补充背景并记住后续行动。知行将这些步骤集中到对话工作台，减少重复表达和信息搬运。')
sub('2 目标用户与场景')
para('面向学生，知行提供课表与考试资料导入、查询和提醒；面向日常出行用户，提供天气、地点、交通与旅行方案相关工具；面向有资料处理需求的用户，提供附件解析和文件生成；面向需要长期跟进的用户，提供目标、定时任务和信息订阅。各场景使用同一账户、对话记录和操作轨迹，降低重复学习成本。')
sub('3 预期价值与推广条件')
para('作品的价值在于把模型理解与真实业务操作结合，让执行结果、生成文件和后续通知回到同一工作台。记忆管理页面支持用户查看个人记忆，工具轨迹和确认卡片使执行过程更易理解。可配置 Skill 支持领域扩展，Web 形态便于在具备浏览器的终端使用。')
para('当前形态适合个人和小规模应用验证。进一步推广需要稳定的模型与业务接口、通知渠道配置和运维保障；效率提升、用户满意度和成本节约应通过后续真实用户试用量化，不预设未经测量的收益。')

page('二 系统架构与模型分工')
para('知行采用 React 18 与 TypeScript 前端、Java 21 与 Spring Boot 3.3 后端。Vite 构建产物由后端静态目录托管，形成一个 Web 应用。下图按职责展示模块依赖，并非将模块拆分为独立微服务。')
figure('知行技术架构图')
para('图1 知行技术架构。聊天与定时任务共享业务能力；通知服务写入 outbox 后调用用户级 SSE 推送。模型、向量检索及第三方信息接口属于外部依赖。')
sub('1 模型负责理解与生成')
para('配置模板将文本模型设为 qwen3.6-plus，视觉模型为 qwen-vl-max，图像模型为 qwen-image-2.0，ASR 为 paraformer-v2，TTS 为 cosyvoice-v3-flash。文本模型通过兼容接口返回正文或结构化工具调用，业务代码负责实际执行。以上为代码配置示例，部署时可调整，不代表各外部服务均已完成在线验证。')
sub('2 存储负责状态与结果')
para('SQLite 保存账户、可见消息、模型上下文、运行状态和业务数据；Qdrant 支撑长期记忆与 Skill 知识检索；ArtifactService 管理上传件与生成产物。长期记忆的 Embedding 示例配置使用 BGE-M3、1024 维；向量模型与知识集合维度需要保持一致。')

page('三 从输入到执行的核心流程')
para('WebChatController 从认证会话取得用户身份，解析或创建所属对话，并建立 runId。ChatApplicationService 解析附件、创建请求级产物收集器，再将消息与身份交给 ReActAgentExecutor。')
figure('知行任务执行流程图',350)
para('图2 内联 ReAct 主执行流程。专用 Skill 执行器可承接独立工作流；已有待确认交互也可在进入普通循环前处理。工具结果回到模型，构成后续判断依据。')
para('对用户而言，这条链路可以概括为“说清目标、补齐条件、调用工具、返回结果”。工具轨迹不是调试信息的堆叠：它让用户知道系统调用了哪些能力、在哪一步等待确认，以及最终产生了什么结果。普通问答通常只交付一段文字；知行还保留本轮可查看的执行过程，使用户可以在已有结果上继续调整任务。')
para('SkillRouter 按显式切换、待处理交互、强规则触发、续接判断及语义路由选择能力。ExecutionLoop 默认限制最多15轮、累计32次工具调用，并处理空回复、结构化计划校验和终止汇总。SafetyPolicy 默认对 file_delete 与 file_update 要求确认；用户可确认或取消，不将所有工具都描述为强制确认。')

page('四 提示词与多模态上下文设计')
sub('1 分层提示词与能力约束')
para('提示词由系统规则、所选 Skill 的场景说明、工具定义和相关知识共同构成。系统规则说明角色与执行要求；Skill 提示词强调旅行、校园、天气等场景的处理步骤；工具定义为模型提供名称、参数与用途。SystemPromptBuilder 注入 Skill 检索知识，避免在上下文组装阶段重复召回。')
para('例如“根据这份课表安排提醒”先需要附件解析，再识别课程信息和缺失条件，最后调用相应业务工具。可靠完成的依据应是工具结果与业务状态，而非模型单纯回复“已经设置”。项目的 SkillReplyGuard 机制及相关测试针对提醒、目标等场景约束结束回复。')
sub('2 历史摘要与长期记忆')
para('DefaultContextBuilder 组合最近对话轮次、当前消息与长期记忆，并支持摘要和按轮次边界的预算裁剪。当前 ReActAgentExecutor 构造时将预算参数传为0，即该入口尚未启用固定 token 预算裁剪；不能将“具备预算组件”写成“已全面启用动态压缩”。')
para('对于“记住……”类明确请求，ChatApplicationService 还提供异步保存路径：匹配短文本后调用 saveManual。该路径降低了对模型主动调用记忆工具的依赖，但仍可能因存储或外部依赖异常失败，因此不承诺记忆绝对不会丢失。长期记忆与会话摘要分别服务于跨轮偏好和历史压缩，职责不同。')
sub('3 附件输入与产物输出')
para('附件先校验用户归属，再按类型处理：图片转为视觉请求，音频转写为文本，文件交由解析服务提取内容。提取结果以附件标识拼接到输入消息；上传处理去重并最多取8件附件。文档解析依赖 Tika、PDFBox 和 POI 等组件，与调用视觉模型属于不同处理路径。')
para('生成图片、语音或文件通过请求级 ArtifactCollector 汇入当轮响应，由前端展示或下载。知行同时提供角色管理界面及音色相关配置；角色界面与声音样本上传的存在，不等于所有自定义声音已具备可用的在线合成能力。')

page('五 工程特点与技术问题')
sub('1 规则与模型联合路由')
para('作品的应用创新在于将可解释的规则触发、会话续接和模型语义路由组合起来，再通过可配置 Skill 限定任务能力。明确切换和待处理交互具有优先级，可以减少多轮任务中错误继承旧场景的问题。此创新属于应用编排与工程实现，不宣称提出新的基础模型算法。')
para('知行把 Agent 能力落成用户可以观察的步骤：路由决定场景，工具负责动作，确认卡片处理高风险操作，运行记录保存结果。这里展示的不是模型内部推理，而是前端能够呈现、用户可以核对的调用状态和业务结果。')
sub('2 可见记录与模型上下文解耦')
para('chat_messages 和 chat_runs 保存用户可见正文、附件、工具轨迹、产物及状态；context_messages 服务于模型上下文。两者分离，使界面记录不必完全依赖用于推理的历史格式。启动迁移会将未完成的 STREAMING 运行标记为 FAILED，并保留中断前内容；这属于结果保留和失败恢复，不等同于自动续跑所有任务。')
sub('3 身份传递与操作确认')
para('Spring Security 使用会话认证、CSRF 和 BCrypt。用户身份传递到会话、记忆、附件和通知服务，关键读写使用用户归属条件；ArtifactService 加入文件访问边界。租户隔离是已实现的设计机制，仍应结合完整接口测试检查覆盖情况。默认高风险工具确认、取消与失败状态在前端以轨迹和卡片反馈。')
sub('4 主动服务闭环')
para('定时任务、课程提醒及订阅服务将通知交给 DatabaseNotificationSink。服务保存 notification_outbox 后直接调用 NotificationStreamService.notify，向该用户在线连接推送；历史和未读记录通过查询接口读取。邮件是按用户设置启用的旁路渠道，并非仅在检测到离线时才发送。邮件异常被隔离处理，避免其失败直接中断站内通知逻辑。')
sub('5 当前边界与改进方向')
para('外部模型与信息接口不可用时，整体体验仍会受限；SSE 连接可能断开，SQLite 形态也需要在扩展并发前评估。通知当前在事务方法内直接推送，尚不能等同于带独立派发器、提交后投递与重试保障的可靠消息队列。后续可优先补充外部依赖监测、通知投递一致性和针对真实场景的端到端评测。')

page('六 验证设计与演示安排')
para('代码库包含 Skill 路由、工具确认、上下文预算、附件归属、会话记录、通知、课程解析和执行循环等测试。测试文件说明这些行为已有验证入口，但不能替代本次实际运行结果；报告不据此声称全部测试通过、上线稳定或取得特定成功率。')
sub('1 场景演示')
para('演示一：登录知行，在对话中上传课程文件，观察解析、补充提问和课表查询结果。演示二：提出包含天气与路线的出行问题，查看 Skill 和工具轨迹，并检查输出是否与接口结果一致。演示三：提出记忆保存请求，在记忆页面核对记录，随后通过新一轮对话检查是否合理召回。')
para('演示四：请求修改或删除一个测试文件，确认界面先出现待确认状态，分别演示取消与允许。演示五：创建短周期测试提醒，在通知中心检查消息；启用邮件时检查旁路投递。演示六：上传图片或音频，核对视觉描述、转写与后续处理是否保持语义一致。以上为建议执行的演示步骤，不写作已经完成的用户试验。')
sub('2 建议评测指标')
para('任务成功率应以业务状态变化和有效产物为标准；延迟记录从请求到首段可见正文及最终完成的时间；记忆评测分别检查保存、召回与无关内容误注入；确认机制检查未授权执行比例；租户隔离以两个测试账户交叉访问验证；通知区分数据库持久化、在线推送和邮件投递结果。')
sub('3 运行与交付')
para('本地运行需要 Java 21、前端构建环境、SQLite 文件目录，以及所启用的模型、Embedding、Qdrant 和第三方接口配置。前端构建后由 Spring Boot 托管。演示数据宜使用不含真实身份信息的样例，模型密钥通过环境变量或本地配置管理，提交材料不附带真实密钥和个人数据。')
para('知行的现阶段交付重点是可组合任务执行、多模态资料处理、可查看的个人记忆和持久化主动通知。后续迭代将以真实使用场景的完成质量为依据，逐步完善成本、延迟、稳定性和通知一致性，而不是仅扩充工具数量。')

editorial={
'以上为代码配置示例，部署时可调整，不代表各外部服务均已完成在线验证。':'各模型通过配置接入，部署时可根据服务可用性与成本调整；运行前需分别检查对应接口。',
'用户可确认或取消，不将所有工具都描述为强制确认。':'用户可确认或取消；其他工具依据风险规则直接执行或记录日志。',
'即该入口尚未启用固定 token 预算裁剪；不能将“具备预算组件”写成“已全面启用动态压缩”。':'即该入口尚未启用固定 token 预算裁剪。后续可依据实际上下文用量评估并启用合适的预算。',
'但仍可能因存储或依赖异常失败，因此不承诺记忆绝对不会丢失。':'保存异常会记录日志，后续需补充可见的失败反馈与补偿机制。',
'角色界面与声音样本上传的存在，不等于所有自定义声音已具备可用的在线合成能力。':'预设音色、样本存档和自定义声音合成应按各自接口条件分别配置与验证。',
'此创新属于应用编排与工程实现，不宣称提出新的基础模型算法。':'这一设计将基础模型的通用理解能力转化为场景化的任务编排与交互能力。',
'测试文件说明这些行为已有验证入口，但不能替代本次实际运行结果；报告不据此声称全部测试通过、上线稳定或取得特定成功率。':'这些测试构成回归验证基础。下一阶段将结合端到端演示记录任务完成率、延迟和失败类型，形成可复核的测评结果。',
'以上为建议执行的演示步骤，不写作已经完成的用户试验。':'上述场景组成后续演示与验收计划，各项结果应留存运行记录和界面证据。',
'而不是仅扩充工具数量。':'并优先完善高频场景的完成质量。',
}
# 按 humanizer-zh 的技术报告规范再做一轮去模板化编辑。保留代码名、
# 配置值和限制条件，主要调整抽象表述、机械排比和过度平滑的段落节奏。
natural={
'知行是一款面向个人学习、工作和生活事务的 Web 智能助理，以“会记忆、会执行、会主动跟进”为产品主线。用户通过浏览器表达目标，系统将自然语言、多模态附件、领域工具和持久化状态连接起来，提供从问题理解、信息补全、实际执行到结果呈现的连续服务。':
'知行是一款运行在浏览器中的个人智能助理。它不只回答问题，还能读取用户上传的图片、音频和文件，调用天气、地图、课表等工具，并把对话、记忆、任务状态和处理结果保存下来。用户说清想做什么，知行负责补齐必要信息、执行步骤并返回结果；需要长期处理的事项，则继续通过任务和通知跟进。',
'个人事务往往跨越多个应用：安排出行需要天气、地点和路线信息，管理课程需要解析文件并配置提醒，长期目标需要多次记录和复盘。仅获得一段建议并不能完成这些事务，用户仍需手动切换工具、补充背景并记住后续行动。知行将这些步骤集中到对话工作台，减少重复表达和信息搬运。':
'很多日常任务不是问一句就结束。安排出行要查天气、地点和路线；导入课表要先读文件，再确认课程信息和提醒时间；长期目标还要在之后的对话中继续记录。普通聊天模型给出建议后，剩下的操作仍由用户完成。知行把这些步骤放进同一个对话工作台，让模型的判断能接到实际工具和后续任务上。',
'面向学生，知行提供课表与考试资料导入、查询和提醒；面向日常出行用户，提供天气、地点、交通与旅行方案相关工具；面向有资料处理需求的用户，提供附件解析和文件生成；面向需要长期跟进的用户，提供目标、定时任务和信息订阅。各场景使用同一账户、对话记录和操作轨迹，降低重复学习成本。':
'学生可以上传课表或考试资料，让知行解析、查询并设置提醒。准备出行时，用户可以在一次对话里查询天气、地点、路线和交通方案。遇到文档、图片或音频资料，也可以直接上传处理。目标、定时任务和信息订阅则用来承接不能在当轮结束的事情。这些功能共用账户、历史对话和工具轨迹，用户不必为每个场景重新学习一套操作方式。',
'作品的价值在于把模型理解与真实业务操作结合，让执行结果、生成文件和后续通知回到同一工作台。记忆管理页面支持用户查看个人记忆，工具轨迹和确认卡片使执行过程更易理解。可配置 Skill 支持领域扩展，Web 形态便于在具备浏览器的终端使用。':
'知行把模型生成的内容和真实操作放在一起。查询结果、生成文件和后续通知都会回到 Web 工作台；记忆页面允许用户查看已经保存的内容；工具轨迹会显示系统调用了什么能力。删除或修改文件等高风险操作会先出现确认卡片。新增领域能力时，可以增加 Skill 配置和工具适配，不需要重写整条对话链路。',
'当前形态适合个人和小规模应用验证。进一步推广需要稳定的模型与业务接口、通知渠道配置和运维保障；效率提升、用户满意度和成本节约应通过后续真实用户试用量化，不预设未经测量的收益。':
'目前的版本适合个人使用和小规模试用。要扩大使用范围，还需要解决模型和第三方接口的稳定性、通知投递以及日常运维问题。项目尚未积累足够的真实用户数据，因此报告不预设效率或成本收益，后续将通过任务完成率、响应时间和用户反馈进行测量。',
'知行采用 React 18 与 TypeScript 前端、Java 21 与 Spring Boot 3.3 后端。Vite 构建产物由后端静态目录托管，形成一个 Web 应用。下图按职责展示模块依赖，并非将模块拆分为独立微服务。':
'前端使用 React 18、TypeScript 和 Vite，后端使用 Java 21 与 Spring Boot 3.3。构建后的前端文件由 Spring Boot 直接托管，因此部署时仍是一个 Web 应用。图中按代码职责划分模块，用来说明调用关系；这些模块并不是独立部署的微服务。',
'提示词由系统规则、所选 Skill 的场景说明、工具定义和相关知识共同构成。系统规则说明角色与执行要求；Skill 提示词强调旅行、校园、天气等场景的处理步骤；工具定义为模型提供名称、参数与用途。SystemPromptBuilder 注入 Skill 检索知识，避免在上下文组装阶段重复召回。':
'发送给文本模型的内容不是一段固定提示词。系统规则先限定角色和执行要求，当前 Skill 再补充旅行、校园或天气等场景的处理方式，工具定义告诉模型可以调用什么、参数怎样填写。需要领域知识时，SystemPromptBuilder 将检索结果加入系统提示；DefaultContextBuilder 不再重复检索同一批知识。',
'例如“根据这份课表安排提醒”先需要附件解析，再识别课程信息和缺失条件，最后调用相应业务工具。可靠完成的依据应是工具结果与业务状态，而非模型单纯回复“已经设置”。项目的 SkillReplyGuard 机制及相关测试针对提醒、目标等场景约束结束回复。':
'以“根据这份课表安排提醒”为例，系统先读取附件，再识别课程和提醒条件。如果时间、学期等信息不完整，就继续向用户询问；信息齐全后才调用课表或提醒工具。系统判断任务是否完成时会检查工具结果和业务状态，不能只接受模型生成的一句“已经设置”。SkillReplyGuard 用于约束提醒、目标等场景的结束回复。',
'作品的应用创新在于将可解释的规则触发、会话续接和模型语义路由组合起来，再通过可配置 Skill 限定任务能力。明确切换和待处理交互具有优先级，可以减少多轮任务中错误继承旧场景的问题。这一设计将基础模型的通用理解能力转化为场景化的任务编排与交互能力。':
'SkillRouter 没有把所有意图判断都交给模型。用户明确切换能力、回复待确认问题或命中高置信度规则时，系统先按确定路径处理；只有这些条件不足时，才使用语义路由。这样做是为了避免多轮对话继续沿用已经失效的场景。选中的 Skill 会缩小本轮可用能力范围，使基础模型的判断落到具体任务步骤上。',
'外部模型与信息接口不可用时，整体体验仍会受限；SSE 连接可能断开，SQLite 形态也需要在扩展并发前评估。通知当前在事务方法内直接推送，尚不能等同于带独立派发器、提交后投递与重试保障的可靠消息队列。后续可优先补充外部依赖监测、通知投递一致性和针对真实场景的端到端评测。':
'系统仍受外部服务影响：文本模型或地图、天气接口不可用时，相应任务无法完整执行；SSE 断开后，用户可能收不到当时的实时事件；SQLite 也不适合在未评估的情况下直接承担更高并发。通知目前在数据库事务中写入记录后立即推送，还没有独立派发器和可靠重试。下一步应先补外部依赖监测、通知提交后投递和端到端场景测试。',
'代码库包含 Skill 路由、工具确认、上下文预算、附件归属、会话记录、通知、课程解析和执行循环等测试。这些测试构成回归验证基础。下一阶段将结合端到端演示记录任务完成率、延迟和失败类型，形成可复核的测评结果。':
'代码库已经为 Skill 路由、工具确认、上下文预算、附件归属、会话记录、通知、课程解析和执行循环编写测试。这些测试主要检查单个模块和关键边界。竞赛演示还需要按完整用户任务记录成功或失败、响应时间以及失败发生在哪一步，才能评价系统在真实流程中的表现。',
'知行的现阶段交付重点是可组合任务执行、多模态资料处理、可查看的个人记忆和持久化主动通知。后续迭代将以真实使用场景的完成质量为依据，逐步完善成本、延迟、稳定性和通知一致性，并优先完善高频场景的完成质量。':
'当前版本已经连通任务执行、多模态资料处理、个人记忆和主动通知。后续开发将围绕高频场景记录完成率、延迟、调用成本和通知一致性，再决定是否扩展工具。',
}
for p in doc.paragraphs:
    new=p.text
    for old,replacement in editorial.items(): new=new.replace(old,replacement)
    for old,replacement in natural.items(): new=new.replace(old,replacement)
    if new!=p.text: set_text(p,new)
doc.core_properties.author=''; doc.core_properties.last_modified_by=''; doc.core_properties.comments=''
doc.core_properties.title='知行作品报告书'
doc.save(out)
print(out)
