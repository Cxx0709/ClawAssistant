# 角色音色定制功能说明书

## 1. 功能概述

本功能为 ClawAssistant 应用提供 AI 角色的音色定制能力，支持预设音色选择和自定义声音克隆。用户可为每个 AI 角色选择系统预设音色，或上传 3-10 秒声音样本，系统通过阿里云百炼声音克隆（voice-enrollment）技术生成专属音色，使 AI 在对话时以角色专属音色进行语音播报，实现"见其人、闻其声"的沉浸式交互体验。

## 2. 功能入口

- 顶部导航栏 → 左侧角色选择下拉框 → 点击「管理角色」
- 进入「AI 角色」页面，点击已有角色卡片或「创建角色」按钮，打开角色编辑面板
- 在编辑面板底部的「声音定制」区域进行音色配置

## 3. 功能模块说明

### 3.1 预设音色选择

**界面位置**：角色编辑面板 → 「选择预设音色」下拉框

**功能描述**：
用户可从下拉列表中选择一个系统预设音色，AI 角色在语音播报时将使用该音色。若选择「不使用」，则表示不使用预设音色，需通过上传声音样本进行声音克隆。

**选项说明**：

| 选项名称 | 对应音色 ID | 音色特点 | 适用场景 |
|---------|------------|---------|---------|
| 不使用 | （空） | 不选预设音色，需上传样本克隆 | 自定义专属音色 |
| 阳光大男孩 | longanyang | 阳光开朗男声 | 年轻男性角色 |
| 欢脱元气女 | longanhuan_v3 | 活泼元气女声 | 年轻女性角色 |
| 天真烂漫女童 | longhuhu_v3 | 天真女童声 | 小女孩角色 |
| 豪放可爱少女 | longxian_v3 | 豪放少女声 | 少女角色 |
| 稚气呆板女童 | longling_v3 | 稚气女童声 | 小女孩角色 |
| 阳光男童声 | longniuniu_v3 | 阳光男童声 | 小男孩角色 |

**底层模型**：所有预设音色统一使用阿里云百炼 `cosyvoice-v3-flash` 语音合成模型。

### 3.2 声音样本上传与克隆

**界面位置**：角色编辑面板 → 「声音定制」区域 → 「上传声音样本」按钮

**功能描述**：
用户上传 3-10 秒的音频文件作为角色声音样本，系统调用阿里云百炼 voice-enrollment 接口进行声音克隆，生成该角色专属的自定义音色 ID。克隆成功后提示"声音上传成功！AI 现在会用这个声音说话了～"。

**操作流程**：
1. 预设音色选择「不使用」
2. 点击「上传声音样本」按钮
3. 在文件选择框中选择音频文件（支持 mp3、wav 等常见音频格式）
4. 系统将音频上传至阿里云文件服务，调用 voice-enrollment 进行声音克隆
5. 克隆成功后，按钮文字变为「重新上传声音」，可点击「🔊 试听声音」预览克隆效果

**技术实现**：
- 音频文件保存至服务器 `data/voices` 目录，通过 `/voices/**` 路径访问
- 调用阿里云百炼声音克隆接口生成自定义音色 ID
- 克隆音色使用 `cosyvoice-v3.5-plus` 模型进行语音合成

### 3.3 试听声音

**界面位置**：角色编辑面板 → 「声音定制」区域 → 「🔊 试听声音」按钮

**功能描述**：
点击后系统调用语音合成接口，以角色当前配置的音色（预设音色或克隆音色）合成一段语音并播放，供用户预览效果。

**使用条件**：角色已选择预设音色或已完成声音克隆时，试听按钮可见。

### 3.4 语音合成（TTS）

**接口路径**：`POST /api/ai-roles/{id}/tts`

**功能描述**：
根据角色 ID 查询其配置的音色，调用阿里云百炼 CosyVoice 模型将文本合成为语音，返回 MP3 音频流。

**音色路由逻辑**：
- 若角色 `voiceId` 属于预设音色列表 → 使用 `cosyvoice-v3-flash` 模型 + 预设音色 ID
- 若角色 `voiceId` 为克隆生成的自定义音色 ID → 使用 `cosyvoice-v3.5-plus` 模型 + 自定义音色 ID
- 若 `voiceId` 为空 → 使用配置文件中的默认音色

## 4. 数据结构

### 4.1 角色表（ai_role）关键字段

| 字段名 | 类型 | 说明 |
|-------|------|------|
| id | String | 角色唯一标识 |
| name | String | 角色名称 |
| voice_id | String | 音色 ID（预设音色 ID 或克隆生成的自定义音色 ID） |
| voice_audio_url | String | 上传的声音样本文件访问路径 |

### 4.2 预设音色常量

```
PRESET_VOICES = {
    "longanyang", "longanhuan_v3", "longhuhu_v3",
    "longxian_v3", "longlaoyi_v3", "longling_v3", "longniuniu_v3"
}
```

## 5. 配置项

| 配置项 | 值 | 说明 |
|-------|-----|------|
| voice.tts-model | cosyvoice-v3-flash | 默认 TTS 模型（预设音色） |
| voice.tts-voice | longanyang | 默认音色 |

## 6. 依赖服务

- **阿里云百炼语音合成**：CosyVoice-v3-Flash（预设音色）/ CosyVoice-v3.5-plus（克隆音色）
- **阿里云百炼声音克隆**：voice-enrollment 接口，根据上传音频生成自定义音色
- **本地文件存储**：`data/voices/` 目录，通过 `/voices/**` 路径映射访问

## 7. 典型使用场景

以创建「奶奶」角色为例：
1. 点击「创建角色」，填写角色名称"奶奶"、性格、背景故事等信息
2. 预设音色选择「不使用」
3. 点击「上传声音样本」，上传一段奶奶的录音
4. 系统进行声音克隆，提示"声音上传成功"
5. 点击「🔊 试听声音」，听到与奶奶声音相似的语音播报
6. 保存角色后，在对话中 AI 回复时自动以克隆的奶奶音色进行语音播报
